# 阶段 F 自动验收报告

日期：2026-08-28

状态：文档与公式自动验收通过，等待人工检查。

## 本阶段范围

阶段 F 只修订并冻结 `算法补充.md` 的数学定义和后续软件接口，不修改现有 C++ 求解器。

## 已完成内容

- 统一规定计算空间步长为 1，Jacobian 为
  $J=\partial(x,y,z)/\partial(\xi,\eta,\zeta)$，并明确 $J$ 具有局部体积尺度意义。
- 统一以 $\boldsymbol S^\xi=J\nabla\xi$、$\boldsymbol S^\eta=J\nabla\eta$、
  $\boldsymbol S^\zeta=J\nabla\zeta$ 表示有向面矢量。
- 规定 `phenglei_refined` 与 `scmm6` 两种可切换度量方法及相同输出语义。
- 修正 SCMM 九个分量中的方向/重复错误，并使用相容的对称散度计算物理 Jacobian。
- 补齐中心插值和通量差分的高、低两端闭合，修正低端第二点差分首项符号。
- 修正五阶线性重构在同一 $j+1/2$ 界面的左右模板索引。
- 补全计算空间无粘通量的面积因子、Riemann/通量分裂接口以及高阶通量散度流程。
- 补全守恒型物理梯度、Stokes 应力、Fourier 热传导、粘性通量及其离散流程。
- 明确物理边界、块间连接、周期连接和 MPI halo 的不同处理路径。
- 给出度量、无粘、粘性和多块/MPI 的后续实现顺序及最低验收项。

## 自动验收结果

- 用精确有理数检查 $I_6$：对 0 至 5 次多项式的半节点插值矩条件全部满足。
- 用精确有理数检查三条低端及三条高端五点插值闭合：对 0 至 4 次多项式全部精确。
- 用精确有理数检查内部 $D_6$：对 0 至 6 次多项式的一阶导数矩条件全部满足。
- 用精确有理数检查两端 $D_4$ 闭合：对 0 至 4 次多项式的一阶导数矩条件全部满足。
- 在 100 组随机三维仿射映射上逐项检查 SCMM 九个分量，结果分别等于解析余因子矢量
  $\boldsymbol r_\eta\times\boldsymbol r_\zeta$、
  $\boldsymbol r_\zeta\times\boldsymbol r_\xi$、
  $\boldsymbol r_\xi\times\boldsymbol r_\eta$。
- 同一批仿射映射上的对称散度 Jacobian 与解析行列式完全一致。
- `git diff --check` 通过；当前 C++ 源码和测试未改变，因此本阶段未重复执行阶段 E 的求解器测试。

## 人工检查重点

- 是否认可两套度量方法的命名和配置值：`phenglei_refined | scmm6`。
- 是否认可 PHengLEI 模式严格保留加密交错工作区，而不改变主网格数据布局。
- 是否认可把当前相邻面有限体积残差保留为基线算子，后续另行实现本文的高阶 WCNS 通量差分算子。
- 是否认可粘性项先采用层流 Navier--Stokes、常黏度/Sutherland 和固定 Prandtl 数的范围。

人工检查通过前不进入下一阶段。
