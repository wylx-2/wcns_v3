# v1.1.0 阶段 R 自动验收报告

状态：**2026-09-11 自动卡口通过；按项目负责人 R→S→T 连续推进授权，R 不设置单独人工停点，
候选 `v1.1.0-r-candidate.1` 将直接非快进合入 `release/v1.1.0` 并进入 S。**

## 1. 范围与规格证据

- 《算法补充》11.4 冻结了流体域外法向、`w_b|S|` 面积元、流体对固体压力/黏性牵引、
  进入壁面的热流、`Cp/Cf/Cd/Cl/Cm` 以及二维单位展向长度语义。
- [`stage-r-design.md`](stage-r-design.md) 冻结配置、权威数据流、排序键、输出 schema 和阈值。
- 边界事件只读已接受状态，并调用与求解器相同的高阶 operand/gradient halo、
  `interpolate_viscous_face_trace`、`apply_viscous_boundary_trace` 和黏性通量链；不使用第一层
  单元剪切/热流近似。
- `output.boundary.*` 进入配置摘要、digest 和 manifest，但不进入 restart signature；缺省
  `enabled=false` 保持 v1.0 行为。
- `cases/manual/case06_2d_naca0012/` 不属于本阶段，保持用户未跟踪状态；本阶段没有读取、修改
  或加入 Git。

## 2. 实现与 Git 审计

| 提交 | 内容 |
|---|---|
| `2e23102` | 冻结 R 设计并补齐《算法补充》11.4.4 |
| `dbda169` | 增加严格边界输出配置、摘要和 digest 语义 |
| `d9db817` | 实现真实边界迹工程量、确定性 MPI 汇总、原子逐面/载荷输出 |
| `9398519` | 增加公式、闭合面、Couette、导热、切分、1/2/4-rank、缩放和 Case07 卡口 |
| `7590440` | 将 Case07 配置和后处理切换到真实边界面输出 |
| `2bdddf5` | 增加二维/三维边界输出及 2-rank 三维覆盖 |

边界逐面文件按 `(patch,source_zone,k,j,i,axis,side)` 排序并拒绝重复键。运行时 leaf 的全局
面索引由 `PartitionLeaf.cells.begin` 恢复；连接和周期面没有物理 patch，不进入积分。二维
文件头写 `force_per_unit_span=true`，三维写 `false`。

远程推送没有执行：此前外部网络写入授权未获批准。本地分支、提交和候选标签完整，远程状态
明确为未同步。

## 3. 自动验收卡口

| 卡口 | 结果 | 证据 |
|---|---:|---|
| 算法规格核验 | 6/6 通过 | `verify_algorithm_spec.py` 文档契约及既有数学恒等式 |
| 单面公式/法向翻转 | 通过 | `Cp`、压力/黏性/总牵引、`Cf`、热流误差不超过 `1e-15` |
| 闭合常压面 | 通过 | 二维圆柱和三维盒的净力低于 `1e-12`，压力+黏性=总量低于 `1e-13` |
| Couette/线性导热 | 通过 | `Cf` 与 `q_wall` 相对解析误差低于 `5e-11`；零量绝对值低于 `1e-13` |
| 原生多块/运行时切分 | 通过 | 四块圆柱及单 zone 32x16 自动切分，原 zone/patch/全局索引唯一 |
| MPI 等价 | 通过 | 单 zone 切分和四块圆柱的 1/2/4-rank 逐面表与载荷误差低于 `1e-12` |
| 三维与量纲缩放 | 通过 | 三维六面输出、2-rank；2D/3D 坐标/面积/力/力矩缩放往返低于 `1e-12` |
| Case07 | 通过 | 实际 48x32 四块 O 网格生成真实 `Cp` 和载荷历史；六份正式配置完成严格 dry-run |
| Release 串行 CTest | 57/57 通过 | 全量功能、失败、输出/重启、强激波和黏性矩阵 |
| Release MPI CTest | 104/104 通过 | 含 1/2/4-rank 边界表/载荷与三维 2-rank |
| 差异审计 | 通过 | `git diff --check` 无错误；仅保留明确排除的用户未跟踪目录 |

串行全量在增加三维卡口前为 56/56、58.58 s，MPI 全量为 102/102、94.26 s；三维新增测试
随后串行 3/3、MPI 5/5 单独通过，最终完整测试清单为上表 57/57 和 104/104，并在候选提交上
重新执行一次最终全量确认。

## 4. Case07 与输出契约

- `cylinder_mach5_euler`、`cylinder_mach5_robust` 输出真实壁压、`Cp`、压力/总牵引和载荷；
  Euler 配置不伪造黏性阻力。
- Re=20/40/100/200 输出 `p_w,T_w,mu_w,Cp,Cf,q_wall`、压力/黏性/总牵引和载荷。
- `analyze_case07.py` 只从 `*.boundary.rN.*` 和 `*.loads.rN.txt` 读取气动力；单元中心场仅用于
  体场、尾迹和探针。v1.0 已归档估算明确标为历史值，不作为 R 证据。
- 每个事件提交一个稳定 TXT/Tecplot 面表；载荷固定列保存压力、黏性和总力/力矩以及
  `Cd/Cl/Cm`。manifest 只登记成功提交的路径。

## 5. 连续推进结论

R 的全部自动卡口通过，没有把未执行的人工审查写成批准。圆柱 `Cp/Cf` 网格敏感性等人工
判断项目保留到连续 R→S→T 完成后的 T→U 人工卡口。依照现有授权，本候选直接合入
`release/v1.1.0`，随后从该合并提交建立 `stage/v1.1.0-s`。
