# WCNS 算法补充与阶段 G 冻结规范

本文给出结构网格 WCNS 求解器的算法约定。阶段 G 的目标是在不修改求解器实现的前提下冻结数学定义、数据合法域、模块职责和验收依据，后续阶段再据此修改程序。

## 1. 坐标、索引与 Jacobian 约定

计算坐标为 $\boldsymbol{\xi}=(\xi,\eta,\zeta)$，物理坐标为 $\boldsymbol r=(x,y,z)$。统一约定

$$
\Delta\xi=\Delta\eta=\Delta\zeta=1,
$$

因此下文所有差分公式均不再显式除以计算空间步长。若以后允许非单位步长，必须在相应方向的离散导数外除以该方向步长，不能只修改部分算子。

求解器中的 Jacobian 定义为无量纲物理映射的 Jacobian：

$$
J=\frac{\partial(x,y,z)}{\partial(\xi,\eta,\zeta)}.
$$

有量纲坐标对应 $J^*=\partial(x^*,y^*,z^*)/\partial(\xi,\eta,\zeta)$；第 1.1 节规定 $J=J^*/L_{ref}^d$。在保持网格定向一致时要求 $J>0$。无量纲 $J$ 没有物理量纲，但其数值和几何意义仍是单位计算单元的局部面积/体积尺度。本文不使用 $1/J$ 表示 Jacobian。

定义三个有向面矢量（余因子矢量）

$$
\boldsymbol S^{\xi}=J\nabla\xi,\qquad
\boldsymbol S^{\eta}=J\nabla\eta,\qquad
\boldsymbol S^{\zeta}=J\nabla\zeta.
$$

记 $\boldsymbol S^\xi=(S_x^\xi,S_y^\xi,S_z^\xi)$，其余方向同理。面矢量模长 $A^\xi=|\boldsymbol S^\xi|$，单位法向为 $\boldsymbol n^\xi=\boldsymbol S^\xi/A^\xi$。

CGNS `GridCoordinates` 给出顶点坐标。以 $\xi$ 方向为例，若真实单元数为 $N_\xi$，则顶点数为 $N_\xi+1$。本文采用以下互不混淆的索引：

- 顶点索引 $I=1,\ldots,N_\xi+1$；
- 真实单元中心索引 $i=1,\ldots,N_\xi$；
- 单元面索引 $i+1/2$，其中物理低、高端面分别为 $1/2$ 和 $N_\xi+1/2$；
- 求解变量只在真实单元和三层状态虚拟单元上存放，低端状态虚拟单元为 $0,-1,-2$，高端为 $N_\xi+1,N_\xi+2,N_\xi+3$。

顶点坐标、真实单元中心坐标、面坐标、面矢量和 Jacobian 是不同拓扑位置的数据，不允许因为数组下标相同而混用。块间连接面不是物理边界；完成轴置换、方向翻转和 halo 映射后按内部连接处理。

### 1.1 无量纲化约定

带星号的量表示有量纲物理量，无星号量表示求解器内部使用的无量纲量。统一采用参考速度 $U_{ref}$、参考密度 $\rho_{ref}$、参考温度 $T_{ref}$、参考长度 $L_{ref}$ 和参考黏性系数 $\mu_{ref}$：

$$
\boldsymbol x=\frac{\boldsymbol x^*}{L_{ref}},\qquad
t=\frac{t^*U_{ref}}{L_{ref}},\qquad
\rho=\frac{\rho^*}{\rho_{ref}},\qquad
\boldsymbol u=\frac{\boldsymbol u^*}{U_{ref}},
$$

$$
T=\frac{T^*}{T_{ref}},\qquad
p=\frac{p^*}{\rho_{ref}U_{ref}^2},\qquad
\mu=\frac{\mu^*}{\mu_{ref}},\qquad
E=\frac{E^*}{U_{ref}^2}.
$$

压力采用动压尺度而不是热力学参考压力。时间尺度为 $L_{ref}/U_{ref}$，守恒量和通量分别按 $\rho_{ref}$、$U_{ref}$ 的相应乘积缩放。网格坐标从 CGNS 读入后必须先除以 $L_{ref}$，再计算所有中心坐标、面矢量和 Jacobian；禁止先计算有量纲度量再只缩放其中一部分。在 $d$ 维中

$$
\boldsymbol S=\frac{\boldsymbol S^*}{L_{ref}^{d-1}},\qquad
J=\frac{J^*}{L_{ref}^{d}},
$$

所以无量纲 $J$ 仍具有计算单元的体积意义。边界条件和初值若按有量纲输入，应在输入层一次性转换；输出层可按同一组参考量恢复量纲。

两个控制参数为

$$
Re=\frac{\rho_{ref}U_{ref}L_{ref}}{\mu_{ref}},\qquad
Ma=\frac{U_{ref}}{c_{ref}}
=\frac{U_{ref}}{\sqrt{\gamma\widetilde R T_{ref}}}.
$$

其中比气体常数

$$
\widetilde R=\frac{R_0}{\widetilde M},\qquad
R_0=8.314\ \mathrm{J/(mol\cdot K)},
$$

$\widetilde M$ 是气体摩尔质量。空气可取 $\widetilde M=0.029\ \mathrm{kg/mol}$，对应 $\widetilde R\approx286.69\ \mathrm{J/(kg\cdot K)}$。采用以上压力尺度时，无量纲理想气体关系和内能为

$$
p=\frac{\rho T}{\gamma Ma^2},\qquad
e=\frac{T}{\gamma(\gamma-1)Ma^2},\qquad
E=e+\frac12|\boldsymbol u|^2.
$$

无量纲声速为 $a=\sqrt{\gamma p/\rho}$，所以参考温度状态的声速为 $1/Ma$。因此 $Ma$ 虽不显式出现在无粘通量的标准守恒形式中，却通过状态方程和总能进入方程；$Re$ 作为粘性通量散度前的系数出现。参考量必须在一次计算中全局唯一，写入日志和检查点，所有结构块及 MPI rank 不得各自推断或修改。

### 1.2 气体模型、派生控制参数与数值阈值

第一版只支持量热完全理想气体：

```text
gas_model = calorically_perfect
gamma > 1
molar_mass > 0 | specific_gas_constant > 0
```

`molar_mass` 与 `specific_gas_constant` 必须且只能给出一个；同时给出或均未给出都拒绝。$\gamma$ 和由该唯一输入确定的 $\widetilde R$ 在一次计算中为常数。二维计算仍保存五分量状态并强制 $w=0$；二维不删除第三个动量分量的存储位置。

五个正参考量 $U_{ref},\rho_{ref},T_{ref},L_{ref},\mu_{ref}$ 是权威输入，$Re$、$Ma$ 和参考动压只能由它们及 `GasModel` 派生。输入文件不得出现 `Re` 或 `Ma` 字段；发现任一字段即拒绝，既不把它作为输入，也不进行输入值一致性断言。初始化后把派生的 $Re$、$Ma$、参考动压、$\widetilde R$ 和五个参考量统一写入 log 与检查点，供用户核查。CGNS 坐标的长度单位必须由 CGNS `DimensionalUnits`/`DimensionalExponents` 或算例参数中的显式 `mesh_length_unit` 给出；两者都存在时必须一致，两者都不存在时拒绝生产运行。

所有数值阈值集中在 `NumericalFloors`，初始化时转换为无量纲值并广播：

```text
density_floor
pressure_floor
temperature_floor
jacobian_absolute_floor
jacobian_relative_floor
face_area_absolute_floor
face_area_relative_floor
reconstruction_scale_floor
reconstruction_epsilon
```

真实单元要求

$$
\rho>\rho_{min},\qquad p>p_{min},\qquad T>T_{min},
$$

以及

$$
J>\max(J_{abs,min},C_JV_{ref}),\qquad
A_f>\max(A_{abs,min},C_AA_{ref,f}).
$$

其中 $V_{ref}$ 是同一真实单元的无量纲有限体积 `reference_volume`，$A_{ref,f}$ 是同一真实面的顶点多边形参考面积，$C_J$、$C_A$ 分别是相对阈值。`reconstruction_epsilon` 是第 4.2 节的严格正无量纲 $\epsilon_w$，不与任何物理量阈值复用。所有阈值必须有限且非负，$\rho_{min},p_{min},T_{min},\epsilon_w$ 必须严格为正；具体默认值属于实现配置，不隐藏在不同算法模块中。

### 1.3 方程源项与可选开关

源项按“单位物理体积内守恒量的变化率”定义。带源项的无量纲 Navier--Stokes 方程为

$$
\frac{\partial U}{\partial t}
+\nabla\cdot\boldsymbol F^c
=\frac1{Re}\nabla\cdot\boldsymbol F^v+\mathcal S,
\qquad
\mathcal S=
\begin{pmatrix}
S_\rho&S_{\rho u}&S_{\rho v}&S_{\rho w}&S_{\rho E}
\end{pmatrix}^T.
$$

二维计算按单位展向厚度解释，仍保留五分量存储；所有二维源项模型必须保证 $S_{\rho w}=0$，不得通过源项驱动被约束为零的展向速度。

静态曲线坐标下的守恒形式和除以 $J$ 后的状态右端分别是

$$
\frac{\partial(JU)}{\partial t}
+\sum_{d\in\{\xi,\eta,\zeta\}}D_d\widehat F_d^c
=\frac1{Re}\sum_{d\in\{\xi,\eta,\zeta\}}D_d\widehat F_d^v
+J\mathcal S,
$$

$$
\frac{dU}{dt}
=-\frac1J\sum_dD_d\widehat F_d^c
+\frac1{ReJ}\sum_dD_d\widehat F_d^v
+\mathcal S.
$$

因此本项目的 `Residual` 固定表示 $dU/dt$：源项装配时直接加 $\mathcal S$，不能再乘 $J$；只有组装 $\partial(JU)/\partial t$ 或全局守恒积分时才使用 $J\mathcal S$。开关和模型列表定义为

```text
enable_source_terms = false | true
source_models = [uniform_conservative | body_force | manufactured | ...]
```

默认 `false`。关闭时模型列表必须为空，源项路径不分配字段、不调用模型并产生逐位为零的贡献；开启时模型列表不能为空。多个模型按配置顺序逐项求值并累加，顺序写入日志和检查点。重启时开关、模型、参数或顺序不一致应拒绝，除非用户显式启动一个新的计算分支。

若有量纲源项为 $\mathcal S^*$，五个分量的无量纲化分别为

$$
S_\rho=\frac{L_{ref}}{\rho_{ref}U_{ref}}S_\rho^*,\qquad
S_{\rho\boldsymbol u}=\frac{L_{ref}}{\rho_{ref}U_{ref}^2}S_{\rho\boldsymbol u}^*,\qquad
S_{\rho E}=\frac{L_{ref}}{\rho_{ref}U_{ref}^3}S_{\rho E}^*.
$$

例如有量纲体力加速度 $\boldsymbol a^*$ 先转换为 $\boldsymbol a=L_{ref}\boldsymbol a^*/U_{ref}^2$，其一致的守恒源项为

$$
\mathcal S_{body}=
\begin{pmatrix}
0&\rho a_x&\rho a_y&\rho a_z&\rho\boldsymbol u\cdot\boldsymbol a
\end{pmatrix}^T.
$$

第一版只允许显式、点值、单元局部的非刚性源项。每个 Runge--Kutta 子步必须用该子步的状态 $U^{(s)}$、子步物理时间 $t^{(s)}$ 和真实单元中心 $\boldsymbol x_c$ 重新计算；不得每个完整时间步只计算一次，也不得读取状态、坐标或二级量的物理 ghost。模型若需要邻域导数、跨块耦合、随机通信或隐式/刚性积分，必须声明为新的耦合算子并另行设计 halo、时间积分和验收，不能伪装成局部 `SourceTermModel`。

源项模型可以提供额外的显式稳定时间步上限；未提供不等于无条件稳定。源项求值及累加后检查 $\mathcal S$ 和 `Residual` 有限，完成每个 RK 子步的候选状态更新后再按 `NumericalFloors` 检查状态。`uniform_conservative` 用于常量守恒源测试，`body_force` 使用上述能量一致形式，`manufactured` 仅用于制造解验证；具体物理模型必须定义所需参数、单位、适用维数和能量闭合，禁止只给动量源而遗漏其应有的能量功。

## 2. 公共线性插值与差分算子

以下算子供 SCMM6 度量、无粘通量散度和粘性通量散度共用。让几何与通量尽量使用相容的离散导数，是维持离散度量恒等式和自由流保持的基本要求。

### 2.1 六阶中心半节点插值 $I_6$

模板完整时：

$$
I_6(q)_{j+1/2}=
\frac{150}{256}(q_j+q_{j+1})
-\frac{25}{256}(q_{j-1}+q_{j+2})
+\frac{3}{256}(q_{j-2}+q_{j+3}).
$$

它对五次及以下多项式在半节点处精确。物理低端使用五点单边闭合：

$$
\begin{aligned}
q_{1/2}&=\frac{315q_1-420q_2+378q_3-180q_4+35q_5}{128},\\
q_{3/2}&=\frac{35q_1+140q_2-70q_3+28q_4-5q_5}{128},\\
q_{5/2}&=\frac{-5q_1+60q_2+90q_3-20q_4+3q_5}{128}.
\end{aligned}
$$

高端采用严格镜像闭合：

$$
\begin{aligned}
q_{N+1/2}&=\frac{315q_N-420q_{N-1}+378q_{N-2}-180q_{N-3}+35q_{N-4}}{128},\\
q_{N-1/2}&=\frac{35q_N+140q_{N-1}-70q_{N-2}+28q_{N-3}-5q_{N-4}}{128},\\
q_{N-3/2}&=\frac{-5q_N+60q_{N-1}+90q_{N-2}-20q_{N-3}+3q_{N-4}}{128}.
\end{aligned}
$$

上述单边式用于几何量、强修正所需的内侧迹或其他没有合法物理 ghost 的字段。第 5.1、6.3 节定义的原始物理量边界面值拥有公共 primitive ghost，应把这些 ghost 作为计算坐标中的状态延拓并使用同一个内部中心 $I_6$ 模板；二者用途不可混淆。

### 2.1.1 CGNS 顶点到单元中心的六阶插值 $I_6^{v\to c}$

`scmm6_wcns` 不使用顶点算术平均生成中心坐标，因为一般光滑映射下该平均只有二阶定位精度。对顶点值 $q_I^v$，内部单元中心仍使用六点对称式

$$
q_i^c=\frac{3q_{i-2}^v-25q_{i-1}^v+150q_i^v+150q_{i+1}^v-25q_{i+2}^v+3q_{i+3}^v}{256}.
$$

低端前两个单元中心只读取真实顶点 $1,\ldots,6$：

$$
\begin{aligned}
q_1^c&=\frac{63q_1^v+315q_2^v-210q_3^v+126q_4^v-45q_5^v+7q_6^v}{256},\\
q_2^c&=\frac{-7q_1^v+105q_2^v+210q_3^v-70q_4^v+21q_5^v-3q_6^v}{256}.
\end{aligned}
$$

高端使用镜像系数。三维按 $\xi\rightarrow\eta\rightarrow\zeta$ 依次应用一维线性插值；由于是张量积线性算子，交换应用顺序应只产生舍入误差。每个活动方向至少需要 6 个真实顶点，即 5 个真实单元。

物理边界条件若需要真实壁面位置，另由边界 CGNS 顶点构造 `BoundaryGeometry`；该位置数据不替代 SCMM 的中心坐标，也不扩展到物理 ghost。

### 2.2 六阶中心通量差分 $D_6$

模板完整时：

$$
D_6(\widetilde E)_j=
\frac{9}{1920}(\widetilde E_{j+5/2}-\widetilde E_{j-5/2})
-\frac{125}{1920}(\widetilde E_{j+3/2}-\widetilde E_{j-3/2})
+\frac{2250}{1920}(\widetilde E_{j+1/2}-\widetilde E_{j-1/2}).
$$

物理低端使用四阶闭合：

$$
\begin{aligned}
D(\widetilde E)_1&=\frac{-22\widetilde E_{1/2}+17\widetilde E_{3/2}+9\widetilde E_{5/2}-5\widetilde E_{7/2}+\widetilde E_{9/2}}{24},\\
D(\widetilde E)_2&=\frac{\widetilde E_{1/2}-27\widetilde E_{3/2}+27\widetilde E_{5/2}-\widetilde E_{7/2}}{24}.
\end{aligned}
$$

其中第二式首项为正号。高端镜像闭合为

$$
\begin{aligned}
D(\widetilde E)_N&=\frac{22\widetilde E_{N+1/2}-17\widetilde E_{N-1/2}-9\widetilde E_{N-3/2}+5\widetilde E_{N-5/2}-\widetilde E_{N-7/2}}{24},\\
D(\widetilde E)_{N-1}&=\frac{\widetilde E_{N-5/2}-27\widetilde E_{N-3/2}+27\widetilde E_{N-1/2}-\widetilde E_{N+1/2}}{24}.
\end{aligned}
$$

采用上述物理边界闭合时，一个方向至少需要 5 个真实单元。小于该尺寸时必须显式拒绝网格，或另选已验证的低阶闭合，禁止发生越界访问。

### 2.3 PHengLEI-WCNS 四阶通量差分 $D_4^{PH}$

PHengLEI 的 WCNS 实际通量差分不是第 2.2 节的 $D_6$，而是

$$
D_4^{PH}(\widetilde E)_i=
\frac98(\widetilde E_{i+1/2}-\widetilde E_{i-1/2})
-\frac1{24}(\widetilde E_{i+3/2}-\widetilde E_{i-3/2}).
$$

在真实物理边界相邻的第一个单元，PHengLEI 将其降为

$$
D_2^{PH}(\widetilde E)_1=\widetilde E_{3/2}-\widetilde E_{1/2},\qquad
D_2^{PH}(\widetilde E)_N=\widetilde E_{N+1/2}-\widetilde E_{N-1/2}.
$$

第二个单元开始即可使用 $D_4^{PH}$，因为其最外侧所需面恰好是物理边界面，不需要边界外的通量、坐标或度量。PHengLEI 还会在几何折角过大或连接非结构块处局部降阶；小型程序第一版至少实现物理边界相邻单元的确定性降阶。

第 2.2 节的 $D_6$ 和本节的 $D_4^{PH}$ 是两套不同算子。在本项目定义的两个 profile 中，禁止把一个算法生成的度量与另一算法的通量差分组合；测试通过与否都不能解除这一配置约束。

### 2.3.1 PHengLEI profile 的中心到面线性插值 $I_4^{PH}$

`phenglei_wcns` 的粘性变量、真实单元梯度和输运系数需要一个明确的中心到面插值，不能借用 `scmm6_wcns` 的 $I_6$。模板完整时采用四阶中心式

$$
I_4^{PH}(q)_{j+1/2}
=\frac{-q_{j-1}+9q_j+9q_{j+1}-q_{j+2}}{16}.
$$

当构造强修正所需的物理边界内侧迹，或处理没有合法状态 ghost 的字段时，物理低端的边界面和第一个内部面只读取真实单元：

$$
q_{1/2}=\frac{35q_1-35q_2+21q_3-5q_4}{16},\qquad
q_{3/2}=\frac{5q_1+15q_2-5q_3+q_4}{16}.
$$

高端严格镜像。第二个内部面开始使用中心式。第 5.1、6.3 节的原始物理量边界面值已有公共 primitive ghost，此时应把 ghost 作为状态延拓并在边界面继续使用内部中心 $I_4^{PH}$，随后再由 `ViscousBoundaryTrace` 强修正。该算子要求每个活动方向至少 4 个真实单元，只用于 `phenglei_wcns` 的线性物性/梯度插值和壁面内部迹，不替代非线性无粘重构。

### 2.4 物理边界闭合与全局守恒权重

第 2.2 节点值闭合和 PHengLEI 的边界降阶在普通等权求和下都不直接伸缩为 $E_{N+1/2}-E_{1/2}$。因此“逐点导数精度”和“离散全局守恒”必须分别验收，不能仅因每个单元都使用面通量就宣称全局守恒。

对两端均为物理边界的单块网格线，把面通量到单元导数写成矩阵 $D\in\mathbb R^{N\times(N+1)}$。初始化时求正的积分权重 $w\in\mathbb R^N$：

$$
D^T w=e_R-e_L,
$$

其中 $e_L$、$e_R$ 分别选择低、高端物理面。必须检查线性系统残差接近舍入误差、解唯一且所有 $w_i>0$。于是

$$
\sum_{i=1}^{N}w_i(D\widetilde E)_i
=\widetilde E_{N+1/2}-\widetilde E_{1/2}.
$$

单块、逻辑矩形拓扑的三维离散积分采用张量积权重：

$$
\mathcal Q(U)=\sum_{i,j,k}w_i^\xi w_j^\eta w_k^\zeta J_{ijk}U_{ijk}.
$$

实现时不构造稠密矩阵：`DerivativeOperator::build(N)` 生成固定带宽系数，用带状 QR/递推在初始化阶段求一次 $w$，并按 `(profile, N, boundary_pair)` 缓存。周期方向使用循环内部算子和 $w_i=1$。

守恒测试检查 $d\mathcal Q/dt$ 与物理边界净通量一致。$J$ 的定义不因权重改变；$wJ$ 是相应边界闭合下的离散体积积分权。若某个尺寸下不存在正权重，则拒绝该 profile/网格组合。

多块连接处不能把独立单块权重简单拼接。先把所有真实单元编为全局行，把每个真实内部面、唯一共享连接面和物理边界面编为全局面自由度，结合第 5.2 节的 `FaceFluxHalo` 与方向变换组装稀疏离散散度矩阵 $\mathcal D$。再求正的真实单元积分权重 $W_c$ 和正的物理边界面切向积分权重 $W_b$：

$$
\mathcal D^TW_c=B^TW_b.
$$

$B$ 对内部面和连接面列为零，对物理边界面给出外法向符号；全周期域右端为零，并额外固定 $\sum W_c$ 的归一化。该系统必须在给定归一化后具有唯一正解且残差接近舍入误差，否则拒绝当前 topology/profile。多块全局守恒量定义为

$$
\mathcal Q(U)=\sum_{c\in\Omega_h}W_{c}J_cU_c,
$$

定义沿物理边界外法向的净面通量 $\widehat F^{net}=\widehat F^c-Re^{-1}\widehat F^v$。启用第 1.3 节源项后，全局平衡必须满足

$$
\frac{d\mathcal Q}{dt}
=-\sum_{b\in\partial\Omega_h}W_{b}\widehat F_b^{net}
+\sum_{c\in\Omega_h}W_cJ_c\mathcal S_c.
$$

关闭源项时第二项严格为零。唯一共享面和 `FaceFluxHalo` 只保证 $\mathcal D$ 使用一致面值，不能单独作为守恒证明；局部源项不需要连接通信，但必须进入全局体积加权平衡。对模板完整的共形块连接不得套用物理边界闭合或擅自降阶。

## 3. 可切换的度量系数算法

用户层配置项定义为：

```text
algorithm_profile = phenglei_wcns | scmm6_wcns
```

两个配置是彼此独立、互斥且不可拆分的完整算法。一次计算必须且只能选择其中一个，初始化完成后冻结；所有结构块和 MPI rank 必须使用同一个 profile：

| 配置 | 度量离散 | 中心到面线性插值 | 通量散度 | 物理边界首单元 |
|---|---|---|---|---|
| `phenglei_wcns` | 加密交错网格 + `GridDelta` | $I_4^{PH}$ | $D_4^{PH}$ | $D_2^{PH}$ |
| `scmm6_wcns` | 共同中心网格 SCMM + $I_6/D_6$ | $I_6$ | $D_6$ | 第 2.2 节单边 $D_4$ |

`phenglei_wcns` 的加密交错网格、`GridDelta` 度量、$I_4^{PH}$、$D_4^{PH}/D_2^{PH}$ 通量差分和相应边界权重是一套整体；`scmm6_wcns` 的共同中心网格 SCMM、$I_6/D_6$ 度量、$I_6$、$D_6$/单边 $D_4$ 通量差分和相应边界权重是另一套整体。禁止把前一套的度量、插值、差分或边界闭合与后一套交叉使用，反向组合也同样禁止。非线性重构和 Riemann 求解器是两套 profile 共同调用的上层模块，不改变 profile 内部线性算子的绑定。

软件内部可以为单元测试拆分类，但 `MetricMethod`、`DerivativeOperator` 和边界闭合对象只能由 `AlgorithmProfile` 工厂共同创建，每个对象携带并校验相同的 profile 标识。用户配置不得分别指定这些部件；检查点必须记录 profile，重启不一致应立即报错。若未来研究新的组合，应定义为第三个完整 profile，而不是放宽这两种 profile 的约束。

两种方法使用同一套输出语义：真实单元中心的 $J$，以及三个真实面族上的 $\boldsymbol S^\xi$、$\boldsymbol S^\eta$、$\boldsymbol S^\zeta$。面积与单位法向由面矢量派生。由顶点多面体公式得到的有限体积体积另存为 `reference_volume`，只用于诊断；不能与高阶 $J$ 共用一个可被静默覆盖的字段。后续通量模块不得根据算法类型改变 Jacobian 含义，也不得要求物理边界外的度量数据。

### 3.1 `phenglei_wcns`

该模式复现 PHengLEI 结构网格高阶度量的基本思路：

1. 输入是 CGNS 顶点坐标。若一个方向有 $N_v=N+1$ 个顶点，则建立尺寸为 $2N_v-1$ 的加密交错序列。原顶点坐标放在奇数位置，偶数位置由相邻位置算术平均得到；加密计算坐标步长为 $1/2$。三维各位置含义为：奇-奇-奇是顶点，偶-偶-偶是真实单元中心，奇-偶-偶、偶-奇-偶、偶-偶-奇分别是 $\xi$、$\eta$、$\zeta$ 面中心。
2. 对坐标、一阶导数、中间乘积和最终 Jacobian 始终调用同一个 `GridDelta` 离散算子。
3. `GridDelta` 在加密序列内部采用

   $$
   D_r q_m=\frac{9}{8}(q_{m+1}-q_{m-1})-\frac{1}{24}(q_{m+3}-q_{m-3}),
   $$

   两端各自最靠近端点的两个内部加密位置采用 $q_{m+1}-q_{m-1}$，端点采用 $-3q_1+4q_2-q_3$ 及其镜像。式中系数已经包含加密步长 $1/2$ 的尺度。
4. 按第 3.3 节的对称守恒形式计算三个真实面族的面矢量，再由对称散度公式计算偶-偶-偶位置的 $J$。
5. 从加密工作区只回填真实单元中心的 $J$ 和真实面上的 $\boldsymbol S$；不回填物理边界外的坐标、Jacobian、面矢量或其他二级量。

每个活动方向至少需要 3 个真实顶点（2 个真实单元）才能安全执行 PHengLEI 的端点/邻点模板；若要出现四阶内部点则需要更大尺寸并由模板范围检查确定。实现时应把加密数组作为块内临时工作区，不改变求解器主网格索引。块内物理边界端点使用 `GridDelta` 的单边式；结构块连接处可在完成顶点/加密坐标 halo 映射后使用内部式。该模式配套使用第 2.3 节 $D_4^{PH}$，用于与 PHengLEI WCNS 结果逐点比对。

PHengLEI 源码中 `GhostMetricsStructHighOrder*` 还会通过镜像构造部分虚拟单元中心和体积；本程序明确不复制这一数据策略。这里借鉴的是其真实网格上的高阶度量算法和 WCNS 差分配对，而不是允许物理边界 ghost geometry 存在。

PHengLEI 在结构块连接通信之后还会用 `ComputeMetricsWithBCforStructHighOrder*` 和 `ComputeJacobianforStructHighOrder*` 重算连接邻域；其 Jacobian 若非正或相对已有有限体积体积偏差超过 20%，会回退到有限体积体积。本程序默认

```text
metric_fallback = strict
```

即报告高阶 $J$、有限体积参考体积及相对差，并拒绝继续；不得静默替换。只有专门进行 PHengLEI 对照试验时才允许 `metric_fallback = phenglei_fv`。因此单块真实网格内部可要求逐点复现 PHengLEI；多块连接邻域只能在明确复制其连接重算和回退设置后要求逐点一致。

### 3.2 `scmm6_wcns`

该模式采用共同的单元中心工作网格，避免三个面族分别构造度量而破坏离散导数的可交换性：

1. 由 CGNS 顶点坐标按第 2.1.1 节张量积 $I_6^{v\to c}$ 构造真实单元中心坐标 $\boldsymbol r_c$。真实物理边界外不构造 $\boldsymbol r_c$。当前程序已有的四/八顶点平均只保留给有限体积基线度量，不能复用于 `scmm6_wcns`。
2. 定义共同的一维中心导数矩阵

   $$
   \delta_\xi=D_{6,\xi}I_{6,\xi},\qquad
   \delta_\eta=D_{6,\eta}I_{6,\eta},\qquad
   \delta_\zeta=D_{6,\zeta}I_{6,\zeta},
   $$

   即先将真实中心值线性插值到真实半节点，再对半节点值求中心导数。物理边界行使用第 2 节的单边插值和单边差分；块连接完成坐标 halo 后使用内部行。
3. 在真实单元中心按第 3.3 节计算九个 $\boldsymbol S_c$ 分量。所有嵌套层次——坐标一阶导数、乘积导数、面矢量散度和 Jacobian——必须调用同一组 $\delta_\xi,\delta_\eta,\delta_\zeta$ 矩阵，满足 SCMM 的 $\delta^1=\delta^2=\delta^3$ 条件。禁止为某个中间量换用另一套“同阶”模板。
4. 在真实单元中心用

   $$
   J_c=\frac13[\delta_\xi(\boldsymbol r_c\cdot\boldsymbol S_c^\xi)
   +\delta_\eta(\boldsymbol r_c\cdot\boldsymbol S_c^\eta)
   +\delta_\zeta(\boldsymbol r_c\cdot\boldsymbol S_c^\zeta)]
   $$

   计算物理 Jacobian。
5. 用同一个 $I_6$ 把 $\boldsymbol S_c^\xi$ 只沿 $\xi$ 插值到真实 $\xi$ 面，把 $\boldsymbol S_c^\eta$ 只沿 $\eta$ 插值到真实 $\eta$ 面，把 $\boldsymbol S_c^\zeta$ 只沿 $\zeta$ 插值到真实 $\zeta$ 面。物理边界面使用第 2.1 节单边式。于是通量散度 $D_6\boldsymbol S_f$ 与构造度量时的 $D_6I_6\boldsymbol S_c$ 完全一致。
6. 只保存真实面 $\boldsymbol S_f$ 和真实单元 $J_c$；中心面矢量、坐标导数、乘积和中间插值均为临时工作区，工作区合法范围不得包含物理边界 ghost。

在二维模式中删除所有 $\zeta$ 操作，使用二维面矢量和二维 Jacobian 公式。块连接处先交换足够的真实单元中心坐标 halo；若连接含轴置换或方向翻转，先根据 CGNS `Transform` 映射到本块逻辑方向。共享连接面采用唯一所有者同步最终面矢量，避免两侧独立舍入造成不守恒。

### 3.3 共同数学恒等式（离散实现仍按 profile 隔离）

以下对称守恒式是两个 profile 共享的连续代数恒等式，但代入其中的网格位置、工作区和离散导数分别由第 3.1、3.2 节完整规定。共享公式不表示允许交换 `GridDelta` 与 $I_6/D_6$，也不构成跨 profile 复用离散中间量的许可。

三个面矢量的九个分量定义如下：

$$
\begin{aligned}
S_x^\xi&=\frac12[(zy_\eta)_\zeta+(yz_\zeta)_\eta-(zy_\zeta)_\eta-(yz_\eta)_\zeta],\\
S_y^\xi&=\frac12[(xz_\eta)_\zeta+(zx_\zeta)_\eta-(xz_\zeta)_\eta-(zx_\eta)_\zeta],\\
S_z^\xi&=\frac12[(yx_\eta)_\zeta+(xy_\zeta)_\eta-(yx_\zeta)_\eta-(xy_\eta)_\zeta],\\[2mm]
S_x^\eta&=\frac12[(zy_\zeta)_\xi+(yz_\xi)_\zeta-(zy_\xi)_\zeta-(yz_\zeta)_\xi],\\
S_y^\eta&=\frac12[(xz_\zeta)_\xi+(zx_\xi)_\zeta-(xz_\xi)_\zeta-(zx_\zeta)_\xi],\\
S_z^\eta&=\frac12[(yx_\zeta)_\xi+(xy_\xi)_\zeta-(yx_\xi)_\zeta-(xy_\zeta)_\xi],\\[2mm]
S_x^\zeta&=\frac12[(zy_\xi)_\eta+(yz_\eta)_\xi-(zy_\eta)_\xi-(yz_\xi)_\eta],\\
S_y^\zeta&=\frac12[(xz_\xi)_\eta+(zx_\eta)_\xi-(xz_\eta)_\xi-(zx_\xi)_\eta],\\
S_z^\zeta&=\frac12[(yx_\xi)_\eta+(xy_\eta)_\xi-(yx_\eta)_\xi-(xy_\xi)_\eta].
\end{aligned}
$$

Jacobian 采用与面矢量相容的对称散度定义：

$$
J=\frac13\left[
D_\xi(\boldsymbol r\cdot\boldsymbol S^\xi)
+D_\eta(\boldsymbol r\cdot\boldsymbol S^\eta)
+D_\zeta(\boldsymbol r\cdot\boldsymbol S^\zeta)
\right].
$$

离散后必须检查 $J>J_{\min}>0$，并报告块号及逻辑索引。禁止通过取绝对值掩盖反向或退化网格。

二维模式应使用二维退化公式，而不是把三维公式中的 $z$ 简单设为常数：

$$
\boldsymbol S^\xi=(y_\eta,-x_\eta,0),\qquad
\boldsymbol S^\eta=(-y_\xi,x_\xi,0),
$$

$$
J=\frac12\left[D_\xi(\boldsymbol r\cdot\boldsymbol S^\xi)+D_\eta(\boldsymbol r\cdot\boldsymbol S^\eta)\right].
$$

两种模式都必须通过恒等映射、一般仿射映射和扭曲网格的自由流测试。恒等映射应得到 $J=1$、$\boldsymbol S^\xi=(1,0,0)$、$\boldsymbol S^\eta=(0,1,0)$、$\boldsymbol S^\zeta=(0,0,1)$。

## 4. 无粘通量

守恒量及 Cartesian 物理通量明确为

$$
U=
\begin{bmatrix}
\rho&\rho u&\rho v&\rho w&\rho E
\end{bmatrix}^{T},
$$

$$
F=
\begin{bmatrix}
\rho u\\ \rho u^2+p\\ \rho uv\\ \rho uw\\ (\rho E+p)u
\end{bmatrix},\quad
G=
\begin{bmatrix}
\rho v\\ \rho uv\\ \rho v^2+p\\ \rho vw\\ (\rho E+p)v
\end{bmatrix},\quad
H=
\begin{bmatrix}
\rho w\\ \rho uw\\ \rho vw\\ \rho w^2+p\\ (\rho E+p)w
\end{bmatrix}.
$$

这里的 $E$、$p$ 和声速采用第 1.1 节的无量纲定义。采用物理 Jacobian 后，三维 Euler 方程的计算空间形式为

$$
\frac{\partial U}{\partial t}
+\frac1J\left(
D_\xi\widehat F+D_\eta\widehat G+D_\zeta\widehat H
\right)=0,
$$

其中

$$
\widehat F=S_x^\xi F+S_y^\xi G+S_z^\xi H,
$$

$\widehat G$、$\widehat H$ 分别使用 $\boldsymbol S^\eta$、$\boldsymbol S^\zeta$。

### 4.1 界面重构

以 $j+1/2$ 同一界面为例，五阶线性左右状态为

$$
\begin{aligned}
U_{L,j+1/2}&=\frac{3U_{j-2}-20U_{j-1}+90U_j+60U_{j+1}-5U_{j+2}}{128},\\
U_{R,j+1/2}&=\frac{-5U_{j-1}+60U_j+90U_{j+1}-20U_{j+2}+3U_{j+3}}{128}.
\end{aligned}
$$

软件接口至少支持

```text
reconstruction = linear5 | wcns_js | weno_z | mdcd | ...
reconstruction_variables = conservative | primitive | characteristic
```

特征重构必须使用法向物理通量 Jacobian $A_n=\partial(F\cdot\boldsymbol n)/\partial U$ 的左右特征矩阵；这里的 $A_n$ 不得与网格 Jacobian $J$ 混称。重构后恢复为守恒变量。

当前 `wcns_js` 的一个标量左状态采用

$$
\begin{aligned}
p_0&=(3q_{j-2}-10q_{j-1}+15q_j)/8,\\
p_1&=(-q_{j-1}+6q_j+3q_{j+1})/8,\\
p_2&=(3q_j+6q_{j+1}-q_{j+2})/8,
\end{aligned}
$$

线性权为 $(d_0,d_1,d_2)=(1,10,5)/16$，Jiang--Shu 光滑度指标为

$$
\begin{aligned}
\beta_0&=\frac{13}{12}(q_{j-2}-2q_{j-1}+q_j)^2
+\frac14(q_{j-2}-4q_{j-1}+3q_j)^2,\\
\beta_1&=\frac{13}{12}(q_{j-1}-2q_j+q_{j+1})^2
+\frac14(q_{j-1}-q_{j+1})^2,\\
\beta_2&=\frac{13}{12}(q_j-2q_{j+1}+q_{j+2})^2
+\frac14(3q_j-4q_{j+1}+q_{j+2})^2,
\end{aligned}
$$

光滑度指标与 $q^2$ 同尺度，不能让同一个绝对 $\epsilon$ 直接作用于密度、速度、动压和特征变量。`ReconstructionScaling` 为每个重构标量提供冻结的正尺度 $s_q$；primitive 默认尺度为 $(1,1,1,1,p_{ref})$，其中 $p_{ref}=1/(\gamma Ma^2)$，conservative 尺度由参考 primitive 状态转换得到，characteristic 尺度由已归一化的特征变换得到。实际权重使用

$$
\bar\beta_k=\frac{\beta_k}{\max(s_q,s_{floor})^2},\qquad
\alpha_k=\frac{d_k}{(\epsilon_w+\bar\beta_k)^p},\qquad
\omega_k=\frac{\alpha_k}{\sum_l\alpha_l},\qquad
q_L=\sum_k\omega_kp_k.
$$

右状态由镜像模板得到。默认 $p=2$；$\epsilon_w$ 是严格无量纲、全局记录的正参数，$s_{floor}$ 来自 `reconstruction_scale_floor`。改变参考量、参考状态、变量空间或特征矩阵归一化后必须重新生成并记录 `ReconstructionScaling`。每个面完成重构后按“原方案 $\rightarrow$ 五阶线性 $\rightarrow$ 相邻单元一阶状态”的顺序检查并回退，直到左右密度和压力均大于 `NumericalFloors` 阈值；每次回退计数。

### 4.2 界面数值通量

令 $A=|\boldsymbol S^\xi|$、$\boldsymbol n=\boldsymbol S^\xi/A$。`RiemannSolver` 接收按 $\boldsymbol n$ 定向的左右守恒状态，返回每单位物理面积的法向守恒通量。Riemann 型计算空间通量为

$$
\widetilde F_{j+1/2}
=A_{j+1/2}\,
\mathcal R(U_L,U_R,\boldsymbol n)_{j+1/2}.
$$

不得把未归一化的 $\boldsymbol S$ 传入一个会自行归一化法向的求解器后遗漏面积因子 $A$。无粘通量只采用 Riemann 求解器路线：

```text
riemann_solver = rusanov | hllc | roe | ...
```

不设计、不实现通量分裂接口。Roe 等可能产生非物理解的求解器必须配置确定性的 HLLC 或 Rusanov 回退。

### 4.3 无粘项逐方向操作

物理边界面在重构后是否再次强制施加边界状态由唯一开关控制：

```text
strong_boundary_face_state = true | false
```

默认值为 `true`。该开关对一个算例的全部物理边界一致生效并写入日志；不得按 MPI rank 或结构块隐式改变。

对每个块和每个逻辑方向执行：

1. 确认三层 halo 已就绪。块间 halo 来自真实 donor 守恒状态；物理边界 halo 以边界条件生成的 $\rho,u,v,w,T$ 为权威内容，并已同步换算 $p,U$。
2. 对所有内部面重构 $U_L,U_R$。面法向始终指向该逻辑坐标增加方向，$L$ 是低索引侧，$R$ 是高索引侧。
3. 对物理边界面先用公共 ghost 完成常规左右重构。若 `strong_boundary_face_state=true`，保留重构得到的内部迹，低端面的外侧 $L$（或高端面的外侧 $R$）由 `InviscidBoundaryFaceState` 依据该内部迹、真实面法向和边界数据重新构造；这一步是重构后的强约束。若开关为 `false`，不再覆写由状态 ghost 重构得到的外侧迹，只执行有限性、正密度和正压力检查。`false` 只表示无粘面状态的弱施加/研究模式，壁面无穿透不保证在离散边界面上精确成立；无滑移 ghost 还可能通过 Riemann 耗散产生非物理无粘切向动量通量，必须单独完成边界收敛、壁面通量和守恒验证。`ViscousBoundaryTrace` 给出的无滑移、等温或绝热约束始终强制施加，不受该开关影响。
4. 读取该真实面的 $\boldsymbol S$，计算 $A$、$\boldsymbol n$，调用唯一的 `RiemannSolver`，再乘以 $A$ 得到计算空间面通量。
5. 只生成从 $1/2$ 到 $N+1/2$ 的真实面通量。物理边界外的 $-1/2$、$N+3/2$ 等通量既不分配也不计算。
6. `phenglei_wcns` 使用 $D_4^{PH}$，物理边界相邻单元使用 $D_2^{PH}$；`scmm6_wcns` 使用 $D_6$ 和第 2.2 节单边闭合。
7. 三个方向的通量散度相加后除以真实单元 $J$。若采用符号约定 $U_t+\nabla\cdot F=0$，无粘残差贡献为 $-J^{-1}\sum D\widehat F$。

当前程序已有的相邻两面有限体积残差可以保留为基线空间算子，但必须与上述两个 WCNS 配置使用不同名称，不能混称或自动回退而不记录日志。

## 5. 物理边界、多块连接与 MPI

### 5.1 物理边界 ghost 与面状态约束

物理边界外不是网格的一部分。物理边界虚拟单元中可用的只有边界条件生成的原始物理状态

$$
P_g=(\rho_g,u_g,v_g,w_g,T_g)^T
$$

及由同一状态确定性换算的压力和守恒量；坐标、度量、梯度及其他二级计算量全部视为不存在。

每个物理边界 patch 同时保存两个不可混用的范围：`AdjacentCellRange` 的法向索引在低端为 0、高端为 $N-1$；`BoundaryFaceRange` 的法向索引在低端为 0、高端为 $N$。两者的切向索引均覆盖相应真实单元。状态镜像读取前者，真实面法向、面积、边界迹和面通量读取后者。CGNS 顶点 `PointRange` 必须分别转换并验证这两个范围。

| 数据类别 | 物理边界 ghost 是否可用 | 说明 |
|---|---:|---|
| 权威原始量 $\rho,u,v,w,T$ | 仅面状 ghost | 由具体边界条件逐层生成；边/角 ghost 不在当前算法合法域内 |
| 由同一原始状态换算的 $p,U$ | 仅同一面状 ghost | 与原始量在一次事务中生成并共享版本；不得独立填充 |
| 由同一状态即时换算的 $a,h$ 等热力学量 | 仅限局部临时使用 | 不形成可供其他模块依赖的 ghost 字段 |
| 顶点/单元中心/面坐标 | 否 | 不镜像、不外推、不分配物理 ghost 坐标 |
| $J$、体积、$\boldsymbol S$、面积、法向 | 否 | 边界面几何来自真实 CGNS 顶点 |
| 梯度、重构权、光滑度指标 | 否 | 不存放 ghost 字段；梯度/重构算子可以读取权威 primitive ghost 后在真实单元/真实面计算 |
| 黏度、热导率、应力、热流、无粘/粘性通量 | 否 | 只在真实单元或真实面计算 |
| 残差、时间步谱半径 | 否 | 只属于真实单元 |

该约束必须由数据结构体现，而不是只依靠调用习惯：`StateHalo` 在每个物理边界上只覆盖“法向三层、所有切向索引均为真实单元索引”的面状 slab；两个或三个方向同时越出真实范围的边/角 ghost 不分配或标记为非法。`Coordinates`、`MetricField`、`GradientField` 和 `FaceFluxField` 的合法范围止于真实顶点、真实单元或真实面。调试构建中对越界拓扑位置进行断言，未分配区可用 NaN/哨兵检测误读。

物理边界 ghost 必须只有一个权威版本，以 $P_g=(\rho,u,v,w,T)$ 为存储真值。每层填充后由 `GasModel` 立即计算

$$
p_g=\frac{\rho_gT_g}{\gamma Ma^2},\qquad
U_g=\left(\rho_g,\rho_gu_g,\rho_gv_g,\rho_gw_g,
\frac{p_g}{\gamma-1}+\frac12\rho_g|\boldsymbol u_g|^2\right)^T.
$$

$p_g$、$U_g$ 可以缓存，但必须与 $P_g$ 在同一事务中刷新并携带同一版本号；禁止分别运行“无粘 ghost 填充”和“粘性 ghost 填充”。块连接 halo 仍以真实 donor 状态映射得到，不调用物理边界算子。

由此得到以下强制规则：

- 几何预处理完全先于物理边界状态填充，并且只读取 CGNS 真实顶点和结构块连接通信得到的几何数据。
- 度量、Jacobian 和任何坐标导数不得读取物理 ghost 状态，也不得构造物理 ghost 坐标。
- 无粘重构和粘性原始量导数必须读取同一版本的 $P_g/U_g$；无粘、粘性通量仍只能存放在真实面。
- 粘性导数可以沿边界法向读取 $P_g$，但梯度只在真实单元/真实面形成；禁止生成或读取 ghost 梯度。坐标变换只使用真实单元或真实边界面的 $J,\boldsymbol S$，绝不要求 ghost 几何。
- 物理边界边、角处若某个切向几何模板也碰到物理边界，则在该切向继续使用单边真实数据模板，不通过镜像坐标补齐。

对当前算法的索引审计结论是：无粘重构逐方向进行，每个面只沿其法向读取三层状态，另外两个切向索引始终位于真实单元范围；粘性法向导数同样只读取当前面的法向 primitive ghost，切向导数使用真实数据单边闭合，真实面循环不延伸到切向 ghost。因此当前算法不会读取边或角状态 ghost，不考虑其填充。

边界处理拆成三个职责，三者不能共用一个含糊的“边界状态”接口：

```text
PhysicalGhostStateOperator  -> 生成无粘/粘性共用的面状 rho,u,v,w,T，并同步换算 p,U
InviscidBoundaryFaceState   -> 原始重构后按开关修正 Riemann 求解器的物理边界外侧迹
ViscousBoundaryTrace        -> 原始导数/插值后在真实壁面强制速度、温度值或法向导数
```

`BoundaryData` 按 patch 保存有明确单位和类型的目标状态、静压/总压、静温/总温、流向、壁面切向速度、壁温以及边界模型枚举；读取后立即无量纲化。静态网格只允许 $\boldsymbol u_w\cdot\boldsymbol n=0$，移动/变形网格不在当前范围内。缺少当前边界模型必需的数据时初始化失败，不使用全局通用 `prescribed_state` 猜测。

状态 ghost 按层镜像对应内点。低端第 $l$ 层使用真实单元 $l$，高端第 $l$ 层使用真实单元 $N-l+1$，$l=1,2,3$。`PhysicalGhostStateOperator` 先生成 primitive ghost：

- 固壁的运动学与热学条件分别处理。对称面或滑移条件只把相对壁面速度的法向分量反号并保持切向分量；无滑移条件取 $\boldsymbol u_g=2\boldsymbol u_w-\boldsymbol u_i$。
- 对称或绝热条件取 $T_g=T_i$；等温条件取 $T_g=2T_w-T_i$。所有固壁的压力采用零法向外推 $p_g=p_i$，再以 $\rho_g=\gamma Ma^2p_g/T_g$ 闭合密度。若任一层得到 $T_g\le T_{min}$ 或 $\rho_g\le\rho_{min}$，当前状态/边界组合失败，禁止用截断值静默改变边界条件。
- 超声速入口：所有 primitive 量取自该 patch 的给定入口状态；超声速出口：逐层取镜像内点 primitive 状态。
- 亚声速入口、出口和远场：以真实面的外法向构造 $A_n$，对内侧状态与目标状态之间的差作特征投影。$A_n$ 的五个特征速度为 $u_n-a,u_n,u_n,u_n,u_n+a$；相对外法向速度小于零的入射特征取目标状态，大于或等于零的出射特征取内侧状态，再用同一组左右特征矩阵恢复 primitive ghost。声速、焓和特征矩阵均由第 1.2 节 `GasModel` 计算。

无粘左右状态和粘性原始量导数首先都使用上述同一 ghost 内容形成“原始”真实边界面结果。`InviscidBoundaryFaceState` 随后使用重构得到的内部迹执行同一超声速/特征规则。固壁强修正后的外侧迹只反射相对法向速度，保持内部迹的 $\rho,p$ 和切向速度；这会移除无滑移 primitive ghost 在 Rusanov/HLLC 中可能造成的非物理无粘切向动量耗散。`strong_boundary_face_state=false` 时保留原始重构结果，因此无滑移壁可能出现该数值耗散；该模式只用于对照研究并必须记录。

`ViscousBoundaryTrace` 不再拥有另一套 ghost，而是接收由公共 primitive ghost 得到的原始面值和原始梯度，再在真实壁面强制 $\boldsymbol u=\boldsymbol u_w$；绝热壁强制 $\partial T/\partial n=0$；等温壁强制 $T=T_w>0$。壁面压力 $p_w$ 取当前 profile 的内部压力边界迹，等温壁再由 $\rho_w=\gamma Ma^2p_w/T_w$ 闭合密度；绝热壁的 $T_w$ 由零法向温度梯度闭合并用同一状态方程得到 $\rho_w$。当前生产算法始终执行粘性强修正，它不受 `strong_boundary_face_state` 控制；若未来研究关闭粘性强修正，必须使用另一个显式开关并单独验收。任何闭合结果必须通过 `NumericalFloors`。

每个面状 slab 的每层填充及 $p_g/U_g$ 换算后检查 $\rho_g>\rho_{min}$、$T_g>T_{min}$、$p_g>p_{min}$ 和全部分量有限。面填充循环必须把切向索引限制在真实单元范围，避免相邻两个边界面写入同一边/角位置，也就不存在“先填哪个面”的覆盖顺序。若未来加入真正多维重构、混合滤波或任何会访问多法向 ghost 的算子，必须由该算子显式声明边/角 halo 需求并另行设计、验证 corner operator；不得把这一扩展偷偷加入当前边界填充器。

### 5.2 块间连接与 MPI

每个连接保存 `ReceiverAdjacentCellRange`、`DonorAdjacentCellRange` 和唯一的 `SharedFaceRange`。前两者法向位于真实边界单元，后者法向位于真实块面；三者均由 CGNS 顶点范围独立推导和验证。消息描述必须包含连接号、消息种类、算法阶段、邻块、两侧面、轴置换、每轴符号、切向范围、数据拓扑位置、层号和分量变换。

同进程复制与 MPI 必须调用同一交换计划。消息种类至少区分 `State`、`GeometryVertex`、`GeometryOperand`、`SharedMetric`、`Gradient`、`InviscidFaceFlux` 和 `ViscousFaceFlux`，并参与 MPI tag 生成，禁止仅靠调用先后复用同一 tag。状态重构固定需要三层 cell halo。

周期连接也使用连接路径，但额外保存

$$
\boldsymbol x_d=Q\boldsymbol x_r+\boldsymbol d,
$$

其中 $Q$ 是正交旋转矩阵，$\boldsymbol d$ 是平移。坐标重合检查先应用该变换；密度、压力和能量标量不旋转，Cartesian 速度和动量按 $\boldsymbol u_d=Q\boldsymbol u_r$ 变换。标量梯度满足 $\nabla\phi_d=Q\nabla\phi_r$，速度梯度满足 $\nabla\boldsymbol u_d=Q(\nabla\boldsymbol u_r)Q^T$。只有 $Q=I,\boldsymbol d=0$ 时才可省略这些数据。

#### 5.2.1 度量初始化的分阶段通信

物理边界永远没有几何 halo；下表宽度只存在于真实块连接：

| profile | 顶点坐标 halo | 每次导数前的操作数 halo | 最终中心量到共享面的 halo |
|---|---:|---:|---:|
| `phenglei_wcns` | 邻块 2 层真实顶点，等价于 3 层加密坐标 | 3 层加密工作点 | 由同一 3 层加密工作区覆盖 |
| `scmm6_wcns` | 邻块 2 层真实顶点 | 5 层真实中心值 | 3 层真实中心值；实现可复用 5 层缓冲 |

`phenglei_wcns` 初始化顺序为：交换两层顶点坐标，构造连接两侧加密坐标；每次调用 `GridDelta` 前交换该次操作数的三层加密 halo；形成九个面矢量分量后同步连接邻域；形成 $\boldsymbol r\cdot\boldsymbol S$ 后再次交换三层操作数并计算 $J$；最后由唯一所有者发布共享面矢量。

`scmm6_wcns` 初始化顺序为：交换两层顶点坐标并计算真实中心坐标；在每次 $\delta=D_6I_6$ 前交换该次操作数的五层中心 halo；先对坐标求导，在真实中心形成 SCMM 乘积；交换乘积 halo 后求外层导数并形成 $\boldsymbol S_c$；形成 $\boldsymbol r_c\cdot\boldsymbol S_c$，再次交换其 halo 后计算 $J_c$；最后交换 $\boldsymbol S_c$ 的三层 halo，用 $I_6$ 生成共享面矢量并由唯一所有者发布。

这里的 `GeometryOperand` 是带阶段号的临时场，不能把坐标 halo 一次交换后假定所有嵌套导数自动有效。若一个操作数模板跨越两个或三个块连接，交换器必须按逻辑方向分阶段传播已经有效的连接 halo，以生成块连接边/角数据；传播范围不得穿过物理边界。初始化时为每个所需索引组合解析唯一 donor 路径，路径缺失、分支或两个路径给出不一致数据时拒绝该 topology/profile。

共享连接面只计算一套面矢量。以较小全局块号、再以连接号作为确定性所有者；接收侧按连接变换得到自身逻辑正方向下的矢量，并验证坐标误差、面积误差、两侧外法向相反以及常量通量误差。

#### 5.2.2 状态、梯度和面通量通信

状态 halo 交换之后，界面本身的左右状态已经足够做一次 Riemann 求解，但这不等于高阶通量差分的面通量模板已经完整。把块连接视为内点时，接口相邻真实单元还会读取邻块内部的半节点通量：$D_4^{PH}$ 最远需要接口外第 1 个邻块内部面，$D_6$ 最远需要第 2 个邻块内部面。仅靠三层状态 halo 在本块重复重构这些远端面既可能模板不足，也会造成共享面两侧舍入不一致。

因此每次生成面通量后必须执行独立的 `FaceFluxHalo` 同步，而不是再次交换半节点状态。把共享面定义为层 0，沿接收块法向向邻块内部依次定义层 1、2：

| profile | `FaceFluxHalo` 合法层 | 用途 |
|---|---|---|
| `phenglei_wcns` | 0、1 | 共享面以及 $D_4^{PH}$ 所需的第 1 个邻块内部面 |
| `scmm6_wcns` | 0、1、2 | 共享面以及 $D_6$ 所需的第 1、2 个邻块内部面 |

每个块先计算其拥有的真实内部面通量；共享连接面只由度量所用的同一所有者计算。接收侧根据 CGNS `Transform` 重排切向索引；若两块逻辑正方向相反，则对五个守恒通量分量统一变号。普通块连接只置换逻辑索引，不交换全局 Cartesian 分量；旋转周期连接还必须用 $Q$ 旋转三个动量通量分量，质量和能量通量保持为标量，再统一施加面方向符号。物理边界没有 `FaceFluxHalo`。

内部面层由其所在块计算并发送，禁止接收块用状态 halo 重复重构。无粘和粘性通量使用不同消息种类，但层宽均由当前 profile 的散度模板决定。`FaceFluxHalo` 使接口行与单块内部行具有相同面值；全局守恒必须另按第 2.4 节对组装后的 $\mathcal D$ 验证，不能只检查共享面相等。

粘性计算先在真实单元形成梯度。`phenglei_wcns` 的 $I_4^{PH}$ 在连接面需要 2 层梯度 halo，`scmm6_wcns` 的 $I_6$ 需要 3 层；物理边界不分配梯度 halo。梯度、无粘面通量和粘性面通量分别使用独立版本号，散度调用前必须验证当前 Runge--Kutta 子步及 profile 对应版本已同步。

### 5.3 每个 Runge--Kutta 子步的数据顺序

```text
交换块间状态 halo
-> 仅在物理边界填充状态 ghost
-> 原地检查真实单元状态合法性
-> 重构真实面左右状态
-> 按 strong_boundary_face_state 开关决定是否强制重构物理边界外侧迹
-> 计算本块拥有的真实面无粘通量
-> 同步共享连接面无粘通量及 profile 所需的 FaceFluxHalo
-> 需要黏性时计算真实单元梯度，按 profile 交换 2 层或 3 层连接梯度并计算本块拥有的真实面粘性通量
-> 需要黏性时同步共享连接面粘性通量及 profile 所需的 FaceFluxHalo
-> 对真实面通量求散度
-> 若 enable_source_terms=true，以当前 RK 子步的 U、x_c、t 计算真实单元源项并直接累加到 dU/dt
-> 检查源项及 dU/dt 的有限性和模型给出的稳定时间步限制
-> 更新真实单元并对候选状态执行 NumericalFloors 检查；ghost 从不进入时间推进
```

这里的两次通量同步是块间连接的数据依赖，不是物理边界处理，也不是重复进行 Riemann 求解。当前有限体积基线算子只用相邻两面，状态 halo 后可在两块各自计算连接面，但目标高阶 WCNS 算子必须采用上述唯一共享面和 `FaceFluxHalo` 方案。

静态网格的坐标和度量只在初始化阶段计算与通信一次。动态网格不在当前阶段范围内。

## 6. 粘性通量

### 6.1 守恒型物理梯度

对任意标量 $\phi$，在离散度量恒等式成立时采用

$$
\phi_x=\frac1J\left[
D_\xi(\phi S_x^\xi)+D_\eta(\phi S_x^\eta)+D_\zeta(\phi S_x^\zeta)
\right],
$$

$\phi_y$、$\phi_z$ 分别替换为相应的 $S_y$、$S_z$ 分量。这里的系数是 $1/J$，不是 $J$。

计算变量为 $q=(u,v,w,T)^T$。在第 1.1 节的无量纲化下，温度必须由

$$
T=\frac{\gamma Ma^2p}{\rho}
$$

恢复，不能继续使用有量纲形式 $T=p/(\rho\widetilde R)$。有量纲热导率满足 $\kappa^*=\mu^*c_p^*/Pr$，其中 $c_p^*=\gamma\widetilde R/(\gamma-1)$。将粘性通量整体提出 $1/Re$ 后，能量粘性通量内的无量纲导热系数定义为

$$
\chi(T)=\frac{\mu(T)}{(\gamma-1)Ma^2Pr}.
$$

黏度模型支持常黏度或 Sutherland 定律。令 $\mu_T^*=\mu^*(T_{ref})$，Sutherland 公式先按有量纲温度定义为

$$
\mu^*(T^*)=\mu_T^*\left(\frac{T^*}{T_{ref}}\right)^{3/2}
\frac{T_{ref}+S^*}{T^*+S^*}.
$$

令 $S=S^*/T_{ref}$ 后，求解器中使用

$$
\mu(T)=\frac{\mu_T^*}{\mu_{ref}}T^{3/2}\frac{1+S}{T+S}.
$$

通常取 $\mu_{ref}=\mu_T^*$，此时前置比例为 1；若两者不同，必须显式保留该比例，Reynolds 数仍只由第 1.1 节给定的 $\mu_{ref}$ 定义。常黏度模型相应为 $\mu=\mu_{const}^*/\mu_{ref}$。必须校验 $T,T_{ref},\mu_{ref},\mu_T^*,S^*,Pr,Re,Ma$ 为有限正数。

### 6.2 应力和热流

采用 Stokes 假设，令 $\theta=u_x+v_y+w_z$：

$$
\begin{aligned}
\tau_{xx}&=2\mu u_x-\frac23\mu\theta,&
\tau_{yy}&=2\mu v_y-\frac23\mu\theta,&
\tau_{zz}&=2\mu w_z-\frac23\mu\theta,\\
\tau_{xy}&=\mu(u_y+v_x),&
\tau_{xz}&=\mu(u_z+w_x),&
\tau_{yz}&=\mu(v_z+w_y).
\end{aligned}
$$

有量纲热流满足 $\boldsymbol q_h^*=-\kappa^*\nabla T^*$。采用本文无量纲量并把 Reynolds 数提出后，守恒方程写为

$$
U_t+\nabla\cdot F^{inv}-\frac1{Re}\nabla\cdot F^v=0,
$$

则物理粘性通量定义为

$$
F^v=
\begin{bmatrix}
0\\ \tau_{xx}\\ \tau_{xy}\\ \tau_{xz}\\
u\tau_{xx}+v\tau_{xy}+w\tau_{xz}+\chi T_x
\end{bmatrix},
$$

$$
G^v=
\begin{bmatrix}
0\\ \tau_{xy}\\ \tau_{yy}\\ \tau_{yz}\\
u\tau_{xy}+v\tau_{yy}+w\tau_{yz}+\chi T_y
\end{bmatrix},\qquad
H^v=
\begin{bmatrix}
0\\ \tau_{xz}\\ \tau_{yz}\\ \tau_{zz}\\
u\tau_{xz}+v\tau_{yz}+w\tau_{zz}+\chi T_z
\end{bmatrix}.
$$

计算空间粘性通量仍按面矢量变换，例如

$$
\widehat F^v=S_x^\xi F^v+S_y^\xi G^v+S_z^\xi H^v.
$$

因此半离散残差中无粘项为 $-J^{-1}D\widehat F$，粘性项为 $+(Re\,J)^{-1}D\widehat F^v$，启用时再按第 1.3 节直接加 $\mathcal S$。$1/Re$ 只能在粘性散度装配时统一乘一次，不得又写入 $\tau$ 或 $\chi$ 造成重复缩放；源项也不得重复乘 $J$。

### 6.3 推荐离散流程

1. 从真实单元守恒量计算 $q=(u,v,w,T)$，并读取与无粘重构相同版本的 primitive ghost。`phenglei_wcns` 用 $I_4^{PH}$、`scmm6_wcns` 用 $I_6$ 得到内部面物理量；物理边界先把 ghost 当作计算坐标中的状态延拓，用相同 profile 的中心模板得到原始面值。这里只读取 ghost 物理量，不读取 ghost 坐标或度量。
2. 在全部真实面形成原始 $q\boldsymbol S$。按第 6.1 节的守恒形式计算真实单元的 $\nabla u,\nabla v,\nabla w,\nabla T$；`phenglei_wcns` 使用 $D_4^{PH}/D_2^{PH}$，`scmm6_wcns` 使用 $D_6$/单边 $D_4$。物理边界面只使用真实 $\boldsymbol S$，因此 primitive ghost 参与原始量差分但不要求任何 ghost 几何。
3. 内部面梯度由真实单元梯度插值：`phenglei_wcns` 使用 $I_4^{PH}$，`scmm6_wcns` 使用 $I_6$。物理边界相邻真实单元的守恒型梯度已经通过步骤 1 的 ghost 延拓面值包含公共 primitive ghost 的影响；原始边界面梯度再由这些真实单元梯度作当前 profile 的单边插值。不得另行填充或读取物理 ghost 梯度。
4. `ViscousBoundaryTrace` 在上述原始面值/梯度形成后执行强修正。先从沿壁面的真实边界迹按当前 profile 求切向导数。Dirichlet 法向导数由边界点 $x_n=0$ 和法向内侧中心 $x_n=1/2,3/2,\ldots$ 上的 Lagrange 多项式在 0 处求导；`phenglei_wcns` 使用边界值加前 4 个中心并要求对 0--4 次多项式精确，`scmm6_wcns` 使用边界值加前 6 个中心并要求对 0--6 次多项式精确。Neumann 条件（如绝热 $\partial T/\partial n=0$）直接给定法向导数；最后由 `BoundaryGeometry` 的法向/切向基变换回 Cartesian 梯度。无滑移只规定速度值，并不意味着速度法向导数为零。
5. 在真实面先插值得到 $T_f$，再计算 $\mu(T_f)$ 和 $\chi(T_f)$，随后形成应力、热流项及三个物理粘性通量，并用该面的 $\boldsymbol S$ 变换为计算空间通量。除常黏度外，不允许把两侧 $\mu$ 的任意平均代替 $\mu(T_f)$；输运系数不在 ghost 中存放。
6. 用与当前算法配置相同的通量差分求粘性散度，并加入黏性时间步限制。

无滑移绝热壁面满足壁面速度和法向温度梯度约束；无滑移等温壁面满足壁面速度和给定温度约束。MPI 连接上先交换原始量 halo 并在真实单元计算梯度，再按第 5.2.2 节交换 profile 指定宽度的梯度 halo。物理边界永远不参与梯度通信或 ghost 梯度填充。

显式推进的局部时间步同时受无粘和粘性谱半径限制。可采用

$$
\Delta t_{ijk}=\frac{CFL}{
\frac1{2J}\sum_{f\in\partial C}
(|\boldsymbol u\cdot\boldsymbol S_f|+a|\boldsymbol S_f|)
+\frac{C_v\nu_{eff}}{2Re\,J^2}\sum_{f\in\partial C}|\boldsymbol S_f|^2},
$$

其中 $\partial C$ 是该单元的六个真实面（二维为四个），$\nu_{eff}=\max(\mu/\rho,\mu/(\rho Pr))$，$1/Re$ 已在时间步分母中显式给出。该式是显式推进的经验性充分限制，不是离散谱半径的恒等式；$C_v$ 必须按 `(profile, dimension, time_integrator)` 分别通过稳定性试验标定并写入配置。MPI 时间步取全部真实单元的全局最小值。

## 7. 软件接口与数据要求

阶段 G 冻结后的实现至少拆分为以下职责：

```text
ReferenceScales          -> 五个参考量输入、单位换算及只读派生 Re/Ma/参考动压
GasModel                 -> gamma 及 molar_mass/specific_gas_constant 二选一；统一完成 U/Q/p/T/a/h 换算
NumericalFloors          -> rho_min, p_min, T_min, J_floor(V_ref), A_floor(A_ref)
ReconstructionScaling    -> 各分量局部尺度与 epsilon_w 的唯一来源
AlgorithmProfile         -> PHengLEIWCNS | SCMM6WCNS
ProfileFactory           -> 成套创建并校验度量、插值、差分和边界闭合，禁止交叉组合
ConnectivityDescriptor   -> 相邻单元范围、共享面范围、Transform、周期 Q/d 和所有者
GeometryHaloPlan         -> 按 profile/stage 生成 GeometryVertex/GeometryOperand 交换计划
MetricMethod             -> PHengLEIRefined | CenterSCMM6（由 profile 约束）
MetricField              -> J（真实单元）, Sxi/Seta/Szeta（真实面），初始化后只读
GlobalConservationWeights -> 组装全局 D/B 并生成 W_c/W_b
BoundaryGeometry         -> 真实物理面的面心、S、A、n 和切向基
BoundaryData             -> 已无量纲化且按边界模型检查完备的 patch 数据
PhysicalGhostStateOperator -> 填充无粘/粘性共用的面状 primitive ghost，并同步换算 p/U
InviscidBoundaryFaceState  -> 按开关生成或保留重构后的 Riemann 外侧迹
ViscousBoundaryTrace       -> 接收公共 ghost 得到的原始结果，只在真实壁面强修正速度/温度值或法向导数
Reconstruction           -> 真实面左右界面状态
ReconstructionSpace      -> conservative | primitive | characteristic
RiemannSolver            -> Rusanov | HLLC | Roe | ...；输出单位法向每单位面积通量
DerivativeOperator       -> PHengLEID4D2 | SCMM6D6D4（由 profile 约束）
FaceFluxHaloExchanger    -> 同步唯一共享面和 profile 所需的邻块内部面通量层
TransportModel           -> constant(mu_const, Pr) | Sutherland(mu_Tstar, Tstar, Sstar, Pr)
ViscousFluxOperator      -> gradients, wall traces, stresses, heat flux, divergence
SourceTermConfig         -> enable_source_terms、模型有序列表、已无量纲参数和重启签名
SourceTermRegistry       -> 创建 uniform_conservative | body_force | manufactured | ...
SourceTermOperator       -> 以真实单元 U/x_c/t 生成并累加 S，声明可选稳定时间步
```

所有交换对象都带 `(profile, Runge--Kutta step, stage, message_kind, version)`。消费者只接受当前版本；初始化临时量与时间推进临时量不能共享可误认的版本空间。`ConnectivityDescriptor` 是所有 cell/vertex/face 范围和轴变换的唯一来源，其他模块不得再次从 CGNS `PointRange` 独立猜测索引。

### 7.1 数据拓扑位置、合法域和生命周期

数据容器必须携带拓扑位置枚举 `Vertex | Cell | XiFace | EtaFace | ZetaFace | BoundarySlab | ConnectionHalo`，索引器在调试构建中检查位置和合法域。主要字段约定如下：

| 数据 | 合法拓扑位置与索引域 | 生产者 | 消费者与生命周期 |
|---|---|---|---|
| `NodeCoordinates` | 真实 CGNS 顶点；块连接上允许初始化期 `GeometryVertex` 临时 halo，物理边界外非法 | `CgnsReader`、`GeometryHaloPlan` | 度量初始化；静态网格冻结后保留真实顶点 |
| `CellCoordinates` | 真实单元中心；无连接或物理 ghost | 当前 profile 的几何初始化 | 局部源项、输出和诊断；静态网格初始化后只读 |
| `CellState` | 真实单元以 $U$ 为真值；块连接三层 $U$ halo；物理边界仅三层权威 primitive slab 及同步派生的 $p/U$，边/角非法 | 初值/时间推进、状态交换、`PhysicalGhostStateOperator` | 无粘重构、粘性原始量导数、状态检查；每个 RK 子步带版本 |
| `reference_volume` | 真实单元 | 顶点多面体诊断 | 网格诊断；不得作为 `J` 的回退值 |
| `J` | 真实单元 | 当前 `MetricMethod` | 残差、积分、时间步；初始化后只读 |
| `Sxi/Seta/Szeta` | 各自从低端 0 到高端 $N$ 的真实面；共享连接面只有一个权威值 | 当前 `MetricMethod`、共享面所有者 | Riemann、梯度、粘性通量、诊断；初始化后只读 |
| `BoundaryGeometry` | `BoundaryFaceRange` 内的真实物理面 | `MetricField` + CGNS patch | 三个边界算子；静态网格初始化后只读 |
| `GeometryWorkspace` | 真实加密点/中心及块连接临时 halo；可含为嵌套模板生成的连接边/角，物理边界外非法 | `GeometryHaloPlan` 分阶段生成 | 当前度量阶段；初始化结束立即释放 |
| `ReconstructedFaceState` | 真实内部面、唯一共享连接面和物理边界面 | `Reconstruction`、`InviscidBoundaryFaceState` | 当前 RK 子步的 `RiemannSolver`；用后释放或换版本 |
| `InviscidFaceFlux`/`ViscousFaceFlux` | 本块拥有的真实面，加接收的连接层；PH 合法层 0--1，SCMM6 合法层 0--2；物理边界外非法 | 通量算子、`FaceFluxHaloExchanger` | 当前 RK 子步的 `DerivativeOperator` |
| `GradientField` | 真实单元，加 PH 两层或 SCMM6 三层连接 halo；物理 ghost 非法 | `ViscousFluxOperator`、梯度交换 | 当前 RK 子步的面粘性通量 |
| `SourceField` | 仅真实单元，可选临时五分量字段；关闭源项时不分配 | `SourceTermOperator` | 诊断及累加到当前 RK 子步 `Residual`；不得跨子步复用 |
| `Residual` | 真实单元，语义固定为 $dU/dt$ | 无粘/粘性散度及可选源项装配 | 时间推进；每个 RK 子步重置 |
| `W_c`/`W_b` | 全局真实单元/真实物理边界面；连接面无 `W_b` | `GlobalConservationWeights` | 守恒积分与验收；初始化后只读 |

`AdjacentCellRange`、`BoundaryFaceRange`、`ReceiverAdjacentCellRange`、`DonorAdjacentCellRange` 和 `SharedFaceRange` 是强类型范围，不能隐式转换。任何字段访问都必须由“字段拓扑位置 + 强类型范围”共同决定；同样的整数三元组不表示相同物理位置。

所有模块共享以下不可变约定：

- $J=\partial(x,y,z)/\partial(\xi,\eta,\zeta)$ 永远是由无量纲物理坐标得到的几何 Jacobian，正值且数值上具有计算单元体积意义；有量纲体积因子为 $L_{ref}^3J$；
- $\boldsymbol S=J\nabla\boldsymbol\xi$ 是带面积尺度的有向面矢量；
- 计算空间步长恒为 1；
- 物理边界闭合与块间 halo 路径明确分离；
- 只有 primitive 状态及其同步派生的 $p/U$ 拥有面状物理边界 ghost；边/角状态 ghost 以及几何、度量、梯度和通量的物理 ghost 均没有合法区；
- 无粘通量只通过 `RiemannSolver` 生成，不存在通量分裂分支；
- 局部源项只访问真实单元的状态、中心坐标和当前子步时间，不拥有任何 ghost 或隐式 MPI 通信；
- 任何算法降阶、正性保护或小网格回退都必须可配置并记录日志。

初始化阶段的具体调用顺序为

```text
读取并验证 CGNS 顶点坐标/连接/边界
-> 读取并广播 ReferenceScales、GasModel、NumericalFloors、BoundaryData 和 SourceTermConfig
-> 拒绝 Re/Ma 输入字段，由参考量和 GasModel 派生并记录 Re/Ma；将坐标和边界数据转换为无量纲量
-> 从 CGNS PointRange 独立构造并验证全部强类型 cell/vertex/face 范围
-> 构造 ConnectivityDescriptor、周期变换、唯一共享面和确定性所有者
-> 选择并冻结唯一 AlgorithmProfile，由工厂成套创建算法部件
-> 按 profile 建立 GeometryHaloPlan，分阶段交换顶点和每个 GeometryOperand
-> 计算真实面坐标/面矢量及真实单元 J，连接面所有者发布唯一共享面矢量
-> 检查 J、面积、周期变换、连接一致性和离散度量恒等式
-> 组装全局 D/B，求解并检查正的 W_c/W_b 与守恒恒等式
-> 冻结静态 MetricField
-> 构造真实 CellCoordinates，创建并校验 SourceTermRegistry
-> 释放 GeometryWorkspace，建立状态/梯度/面通量的运行期交换计划
```

## 8. 后续实现顺序与验收标准

后续阶段的任务拆分、Git/GitHub 工作流、正式验收记录和人工放行规则以 `docs/development-roadmap.md` 为强制执行依据。本节固定数学实现顺序和最低数值验收项；两份文档必须同时满足。

后续阶段必须逐阶段人工批准，不能因自动验收通过而自行跨阶段：

1. **阶段 H：核心热力学与数据合法域。** 实现 `ReferenceScales`、`GasModel`、`NumericalFloors`、强类型范围、拓扑位置、运行期版本检查以及 `SourceTermConfig/Registry` 空框架；源项默认关闭，不改变现有有限体积推进结果。
2. **阶段 I：两套几何 profile。** 实现 `ProfileFactory`、`GeometryHaloPlan`、两种 `MetricMethod`、共享面所有者和 `GlobalConservationWeights`；只做网格/度量测试，不接 WCNS 流场。
3. **阶段 J：无粘 WCNS 与显式源项。** 实现 profile 专属线性算子、非线性重构、Rusanov 起步、三个边界算子中的无粘部分、`FaceFluxHalo`，以及 `uniform_conservative/body_force/manufactured` 的逐 RK 子步源项装配；完成一维/二维无粘收敛、自由流和有源制造解测试。
4. **阶段 K：粘性 WCNS。** 实现守恒型梯度、连接梯度交换、输运模型和 `ViscousBoundaryTrace`；完成 Couette 流、导热或制造解验证。
5. **阶段 L：算法扩展与健壮性。** 增加 HLLC、特征重构、WENO/MDCD、正性回退和可诊断降阶，不改变两个 profile 的几何/差分绑定。
6. **阶段 M：多块/MPI 系统验收。** 完成串行单块、串行多块和多 rank 等价性、全局守恒、周期连接和重启一致性测试，并整理用户文档与基准结果。

阶段 G 规格和后续实现的最低验收项：

- 恒等映射逐点得到 $J=1$ 和三个笛卡尔面矢量；
- 一般仿射映射逐点得到解析常数 Jacobian 和余因子矢量；
- 光滑扭曲网格上离散度量恒等式残差随加密下降；
- 均匀流在单块、多块及 MPI 分区下保持到舍入误差量级；
- 任一 $J\le$ `NumericalFloors.J_floor(reference_volume)` 或 $A\le$ `NumericalFloors.A_floor(reference_area)` 时确定性报错，而非继续计算。
- 将物理边界外的非状态内存填充为 NaN/哨兵后，全部度量、无粘和粘性边界测试仍通过，证明算法没有读取 ghost 坐标、度量或梯度。
- 将物理边界边/角状态存储填充为 NaN/哨兵后，无粘和粘性边界测试仍通过，证明当前逐方向算法只读取面状状态 ghost。
- 两个 profile 分别达到其声明的内部阶数；物理边界附近按各自闭合得到预期的全局收敛阶。
- 用有理数矩检验逐项验证 $I_4^{PH}$、$I_6$、$D_4^{PH}/D_2^{PH}$、$D_6$/单边 $D_4$ 以及两套壁面法向导数模板所声明的多项式精确度。
- 人为请求任一跨 profile 的度量/差分/边界组合时初始化必须失败；多块或重启 profile 不一致时也必须失败。
- 分别以 `strong_boundary_face_state=true/false` 验证无粘边界路径；默认强约束模式必须逐面满足相应无粘约束，弱模式必须在日志中可辨识并有独立误差记录；两种模式下粘性壁面迹都必须满足给定条件。
- 对每个允许的单块网格线尺寸验证 $D^Tw=e_R-e_L$、$w_i>0$；对完整多块拓扑验证 $\mathcal D^TW_c=B^TW_b$、正权重、唯一性和非零物理边界通量下的加权全局守恒。
- 相同物理网格分别按单块和多块切分后，检查共享面矢量、共享面通量、profile 对应的层 0--1 或 0--2 `FaceFluxHalo`、自由流残差、加权守恒量和一个时间步后的状态差异。
- 分别构造轴置换、轴反向和平移/旋转周期连接，验证坐标变换、Cartesian 状态/梯度旋转、面通量方向和 MPI tag/版本隔离。
- 构造需要跨两个及三个连接传播的度量模板，验证每个 `GeometryOperand` 阶段均得到唯一 donor 路径；缺失、分支或不一致路径必须在初始化时失败。
- 同一物理算例用两组一致的单位和参考量输入时，无量纲初值、$Re$、$Ma$ 和推进结果必须一致；检查粘性残差中 $1/Re$ 只出现一次。
- 输入文件含 `Re` 或 `Ma` 时必须失败；只给五个参考量及唯一气体常数输入时，检查 log/检查点中的派生 $Re$、$Ma$ 和参考动压准确且所有 rank 一致。
- `enable_source_terms=false` 时不得分配或调用源项对象，且与未编译源项路径的基线结果逐位一致；开启但模型列表为空、参数单位缺失或模型不支持当前维数时初始化失败。
- 在周期均匀网格上以常量守恒源验证每个 RK 子步的时间精度及 $\sum W_cJ_c\mathcal S_c$ 全局平衡；以 `body_force` 验证动量源和能量功严格一致，以 `manufactured` 验证空间/时间收敛阶。
- 同一有源算例按单块、串行多块和 MPI 多块运行时，局部源项、全局体积加权源和更新结果在规定容差内一致；源项配置必须写入日志和检查点并参与重启一致性检查。
- `phenglei_wcns` 单块内部与 PHengLEI 对照；连接邻域分别在 `strict` 和显式 `phenglei_fv` 回退配置下记录差异，禁止混合结果。

## 9. 算法来源说明

PHengLEI 的实际 WCNS 调用链为：FD 网格进入 `StructGrid::ComputeMetrics`，非 HDCS 分支调用 `ComputeMetricsStructHighOrder2D/3D`；区域初始化对 `str_highorder_solver` 前缀为 `WCNS` 时继续执行连接坐标、度量和 Jacobian 的通信/更新；流场残差由 `NSSolverStructFD::FluxDifference_WCNS` 使用 $D_4^{PH}$，并依据 `DiscretePrecision*` 在物理边界附近降阶。

因此 `phenglei_wcns` 不是与 SCMM 无关的另一套连续公式：PHengLEI 本身也在加密交错网格上使用第 3.3 节等价的对称守恒构造。两种 profile 的差别主要是几何数据布局、离散导数和边界闭合。本文已按 CGNS 顶点输入修正原先“坐标位于单元中心”的错误，并明确不继承 PHengLEI 镜像生成物理 ghost 单元中心/体积的实现细节。

`scmm6_wcns` 使用共同单元中心工作网格和统一的 $I_6/D_6$ 线性算子实现。两种 profile 共享同一物理 Jacobian 和真实面矢量定义，以便后续程序安全切换和对照验证。

## 10. 核对依据

- PHengLEI 本地源码：`API/Geometry/src/StructGridFD.cpp` 中 `ComputeMetricsStructHighOrder2D/3D`、`GridDelta`、`ComputeMetricsWithBCforStructHighOrder*`、`ComputeJacobianforStructHighOrder*`；`CFD/NSSolver/src/NSSolverStructFD.cpp` 中 `FluxDifference_WCNS`。
- X. Deng 等，*Further studies on geometric conservation law and applications to high-order finite difference schemes with stationary grids*，Journal of Computational Physics 239 (2013) 90--111，DOI: `10.1016/j.jcp.2012.12.002`。
- X. Deng、Y. Chen，*A novel strategy for deriving high-order stable boundary closures based on global conservation, I: Basic formulas*，Journal of Computational Physics 372 (2018) 80--106，DOI: `10.1016/j.jcp.2018.06.012`。
- X. Deng 等，*High-order finite difference schemes based on symmetric conservative metric method: decomposition, geometric meaning and connection with finite volume schemes*，Advances in Applied Mathematics and Mechanics 12 (2020) 436--479，DOI: `10.4208/aamm.OA-2017-0243`。
